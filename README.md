# NetVision---Custom-Network-Packet-Analyzer
A custom GUI-based network packet analyzer built with Python and Scapy to capture, filter, and dissect live HTTP, DNS, TCP, and ICMP traffic at the OSI layer level.
